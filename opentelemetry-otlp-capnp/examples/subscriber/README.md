# Temp Example for Developing a Subscriber

- Develop a Subscriber that exports using Cap'n Proto. 
- move this into a standalone create that can be used with `tracing` and `tracing_subscriber` but does not require OTEL API compatability

